import React from 'react';

const Analytics = () => {
	return (
		<div>
			<h1>Analytics</h1>
			{ /*
      'anaytics' => [
          'enable_tracking'          => true,   // Enable analytics tracking
          'track_page_feedback'      => true,   // Count feedback per page
          'track_feedback_duration'  => true,   // Store duration for stats
          'track_resolved_status'    => true,   // Track resolved/unresolved submissions
          'track_user_type'          => true,   // Logged-in vs guest
          'dashboard_chart_period'   => '30d',  // Default chart period: 7d, 30d, 90d, etc.
          'show_top_pages'           => 5,      // Show top N pages by feedback
          'show_average_duration'    => true,   // Calculate average duration
          'enable_export'            => true,   // Export stats as CSV/JSON
      ]
      */ }
		</div>
	);
};

export default Analytics;
